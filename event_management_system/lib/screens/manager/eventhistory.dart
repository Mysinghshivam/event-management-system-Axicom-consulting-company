import 'package:flutter/material.dart';

class ManagerEventHistory extends StatelessWidget {
  const ManagerEventHistory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Client Event History"));
  }
}
